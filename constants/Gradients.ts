/** Shared gradients keep brand treatment consistent across every surface. */
export const BRAND_GRADIENT = ['#FF0000', '#A61B1B'] as const;

/** A longer red lead suits wide controls without ending in near-black. */
export const BRAND_GRADIENT_EXTENDED = ['#FF0000', '#FF0000', '#A61B1B'] as const;

/** Destructive actions remain distinct while avoiding a muddy black-red stop. */
export const DESTRUCTIVE_GRADIENT = ['#EF4444', '#B91C1C'] as const;
